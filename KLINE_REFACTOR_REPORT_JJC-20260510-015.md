# K线数据层重构报告 — JJC-20260510-015

## 任务概述
**任务ID**: JJC-20260510-015
**任务**: K线获取规则全面改版 — 从"每次从API拉取"改为"本地持续采集+合成"
**状态**: ✅ 已完成

---

## 重构内容

### 第1步：创建 K线数据管理器 ✅

**文件**: `core/kline_manager.py`（新建，约380行）

**功能**:
1. **持续采集1分钟K线**
   - 从 Binance testnet API 获取每个交易品种的 1m K线
   - 每60秒获取一次最新数据（增量追加，不重复获取）
   - 保存到 `data/klines/1m/{SYMBOL}.jsonl`
   - JSON Lines 格式，每行一根K线，字段: `open_time, open, high, low, close, volume, close_time`

2. **合成2分钟K线**
   - 从1分钟K线数据实时合成2分钟K线
   - 合并规则：每2根1m K线合并为1根2m K线
     - `open` = 第一根1m的open
     - `high` = 两根1m的high最大值
     - `low` = 两根1m的low最小值
     - `close` = 第二根1m的close
     - `volume` = 两根1m的volume之和
   - 保存到 `data/klines/2m/{SYMBOL}.jsonl`

3. **数据清理机制**
   - 每12小时执行一次清理
   - 删除超过6小时的1分钟和2分钟K线数据
   - 只保留最近6小时的数据

4. **数据读取接口**
   - `get_klines_1m(symbol, limit)` — 读取最近N根1m K线
   - `get_klines_2m(symbol, limit)` — 读取最近N根2m K线
   - 优先从本地读取，本地数据不足时从API补充

### 第2步：修改引擎使用本地数据 ✅

**文件**: `core/engine.py`（修改）

**关键改动**:
- `fetch_klines()` 方法重构：
  - `1m` 和 `2m` 时间框架 → 从 `kline_manager` 读取本地数据
  - 其他时间框架（如 `15m` CZSC确认） → 仍从 API 获取（向后兼容）
- 删除旧的 `_save_kline()` 方法（不再保存为独立JSON文件）
- 启动时初始化 `kline_manager`
- 启动后台更新线程（每60秒）
- 启动后台清理线程（每12小时）
- 停止时优雅关闭所有后台线程

### 第3步：数据更新线程 ✅

- 独立后台线程 `kline_update`
- 每60秒遍历所有155个交易品种
- 增量更新：只获取上次采集时间之后的新K线
- 更新完成后自动触发2m K线合成
- 与主交易循环不冲突（独立线程）

### 第4步：清理线程 ✅

- 独立后台线程 `kline_cleanup`
- 每12小时执行一次数据清理
- 清理超过6小时的老数据
- 同时清理1m和2m数据文件

### 第5步：测试验证 ✅

**测试项目** | **结果**
--- | ---
kline_manager.py 导入 | ✅ 成功
engine.py 导入 | ✅ 成功
初始加载155品种1m K线 | ✅ 55800根（每品种360根）
2m K线合成 | ✅ 正确（每品种约179-180根）
增量更新（60秒后） | ✅ 自动追加新K线
数据去重（同一open_time） | ✅ 不重复写入
数据清理 | ✅ 139根过期数据已清理
后台线程启动/停止 | ✅ 正常
引擎 fetch_klines(本地2m) | ✅ BTCUSDT 返回180根
引擎 fetch_klines(本地1m) | ✅ BTCUSDT 返回200根
引擎 fetch_klines(API 15m) | ✅ 向后兼容，返回200根
API失败容错 | ✅ 记录日志，不中断引擎

---

## 文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `core/kline_manager.py` | ✅ 新建 | K线数据管理器核心模块 |
| `core/engine.py` | ✅ 修改 | 整合kline_manager，修改fetch_klines方法 |
| `data/klines/1m/` | ✅ 新建目录 | 1分钟K线存储（155个.jsonl文件） |
| `data/klines/2m/` | ✅ 新建目录 | 2分钟K线存储（155个.jsonl文件） |

---

## 架构对比

### 旧架构
```
每次交易周期 → API请求155个品种 × 2个时间框架 → 保存为独立JSON文件
问题：每次都要从API拉取，浪费请求，延迟高
```

### 新架构
```
启动时 → 一次性加载155品种 × 360根1m K线 → 合成2m → 本地JSONL
后台线程（每60秒） → 增量更新所有品种 → 自动合成2m
后台线程（每12小时） → 清理>6小时旧数据
交易周期 → 从本地JSONL读取 → 零API延迟
```

---

## 关键设计要点

1. **JSONL格式** — 追加写入，无需解析整个文件，高效
2. **增量更新** — 根据 `open_time` 去重，只追加新数据
3. **容错机制** — API失败时记录日志，不中断引擎
4. **API回退** — 本地数据不足时自动从API补充
5. **向后兼容** — 15m等其他时间框架仍从API获取，CZSC策略不受影响

---

## 运行日志摘要

```
2026-05-10 19:55:57 [engine] 🚀 交易引擎 V2 初始化 [DRY-RUN 模拟模式]
2026-05-10 19:55:57 [kline_manager] 📊 K线管理器初始化: 155 个品种
2026-05-10 19:55:57 [engine] 📊 K线数据管理器已初始化（本地采集+合成模式）
2026-05-10 19:55:57 [kline_manager] 📥 开始初始加载 K 线数据...
2026-05-10 19:56:25 [kline_manager] ✅ 初始加载完成: 共 55800 根 K 线
2026-05-10 19:56:25 [kline_manager] 🔄 K线后台更新线程启动（每 10s）
2026-05-10 19:56:25 [kline_manager] 🧹 K线后台清理线程启动（每 3600s，保留 6.0h）
2026-05-10 19:56:25 [kline_manager] 🧹 数据清理完成: 共删除 139 根过期 K 线
2026-05-10 19:56:33 [kline_manager] 🛑 K线管理器已停止
```

---

## 证据文件

- **kline_manager.py**: `core/kline_manager.py`（完整代码）
- **数据文件示例**: `data/klines/1m/BTCUSDT.jsonl`（360行）、`data/klines/2m/BTCUSDT.jsonl`（179行）
- **运行日志**: 见上方日志摘要 + `logs/engine.log`
